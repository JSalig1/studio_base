<h2><%= @project.project_number %></h2>

<div>
  <h3>Archived in:</h3>
  <ul>
    <% @drives.each do |drive| %>
      <li><%= link_to "#{drive.number}", drive %></li>
    <% end %>
  </ul>
  <%= link_to 'Add Archive', new_archive_path(project_id: @project) %>
  <%= link_to 'Edit', edit_project_path(@project) %>
</div>
